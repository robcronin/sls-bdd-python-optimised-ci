{
  "name": "python",
  "version": "1.0.0",
  "description": "Python BDD",
  "license": "MIT",
  "scripts": {
    "deploy": "sls deploy",
    "teardown": "sls remove",
    "test:behave": "pipenv run behave --no-capture"
  },
  "devDependencies": {
    "serverless": "^1.57.0"
  }
}
